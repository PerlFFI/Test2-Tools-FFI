use strict;
use warnings;

our $DIR;

{
  cflags => "-Ishare/include",
  # TODO: shoudln't need to hard code these paths
  libs => ["-Lshare/lib", "-Lblib/lib/auto/share/dist/Test2-Tools-FFI/lib", "-lt2t"],
  source => [ "$DIR/*.c", "$DIR/*.cxx" ],
}
