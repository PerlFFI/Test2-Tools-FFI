use strict;
use warnings;

our $DIR;

my @libs;
# TODO: FFI::Build should do this automatically.
push @libs, '-install_name', '@rpath/libt2t.dylib' if $^O eq 'darwin';

{
  verbose => 1,
  cflags => "-Ishare/include",
  libs => \@libs,
  source => ["$DIR/*.c"],
}
